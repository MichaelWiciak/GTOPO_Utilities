To compile the program, execute the "make" command.

To run my test script, type "./testscript.sh"

To run a specific program, please look at the list below:

Usage: ./gtopoEcho inputFile width height outputFile

Usage: ./gtopoComp firstFile width height secondFile

Usage: ./gtopoReduceinput width height reduction_factor output

Usage: ./gtopoTile inputFile width height tiling_factoroutputFile_<row>_<column>

Usage: ./gtopoAssemble outputFile width height (row column inputFile widthheight)+

Usage: ./gtopoPrintLand inputFile width height outputFile sea hill mountain

Usage: ././gtopoAssembleReduce outputArray.gtopo width height (row column inputArray.gtopowidthheight)+

Please note that Git occasionally messes up my indentation on the web application.
In order to mark for code quality, please download the submission and open it in any editor.
Then the indentation error is gone. 
